#include <18F2685.h>
#fuses HS, NOWDT, NOPROTECT, NOLVP
#use delay(clock=20000000)

#define LED1 PIN_B2
#define LED2 PIN_B3

void main(void) {
    setup_adc(ADC_OFF);
    
    output_low(LED1);
    output_low(LED2);
    
    while(TRUE) {
        delay_ms(100);
    }
}
